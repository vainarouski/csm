{
  "type": "object",
  "properties": {

    "files": { "$ref": "files.json" },

    "thumbnail": { "$ref": "thumbnail.json" }
  },

  "required": ["files", "thumbnail"],
  "additionalProperties": false
}